import { Typography } from '@material-ui/core';
import React from 'react'

function Profiles() {


  return (

    
    <div>
    <Typography   paragraph>This is Profiles page</Typography>
    <Typography paragraph>
        If your logic is fetching new oh1tions on each keystroke and using the
        current value of the textbox to filter on the server, you may want to
        consider throttling requests.If your logic is fetching new options on
        each keystroke and using the current value of the textbox to filter on
        the server, you may want to consider throttling requests.
      </Typography>
    </div>
  )
}

export default Profiles;